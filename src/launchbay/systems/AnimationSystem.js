/**
 * src/systems/AnimationSystem.js
 *
 * Wraps THREE.AnimationMixer per character and exposes a small state-machine (idle/walk/run/attack/ability/dodge/death).
 * Status: stub — implemented in Stage 4 (GLB loading) with fallback procedural idle.
 *
 * Provides play(name, { fade, loop }) and update(dt); falls back to a procedural idle bob/breathe when a GLB has no clips.
 */

export {};
